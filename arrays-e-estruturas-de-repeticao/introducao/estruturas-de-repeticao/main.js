/* 

# Estruturas de Repetição: São estruturas utilizadas para redução de código, percorrer listas, gerar melhor interação com o usuário e determinar condições.

1° Variável de controle, 2° Condicional, 3° Incremento ou decremento.

*/

// Estrutura de repetição for: Muito utilizada para percorrer um número de vezes já determinado.

const nome = 'Felipe Lamas';

for (let i = 0; i < nome.length; i++) {
    const letra = nome[i];
    console.log(letra);
}
